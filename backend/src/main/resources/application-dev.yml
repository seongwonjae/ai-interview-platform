spring:
  datasource:
    url: jdbc:mysql://mysql:3306/interview_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: app
    password: app
  data:
    redis:
      host: redis
      port: 6379

ai:
  provider: dummy
